% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bcrat.R
\name{bcrat}
\alias{bcrat}
\title{BCRAT 5-year invasive breast cancer risk}
\usage{
bcrat(
  age,
  race,
  age_menarche,
  age_first_birth,
  n_biopsies,
  n_relatives,
  atypical_hyperplasia = "unknown",
  hispanic_nativity = NULL,
  asian_subgroup = NULL
)
}
\arguments{
\item{age}{Age in years, as a whole number from 35 to 85 (the range NCI's
own tool supports; the underlying model is undefined outside it).}

\item{race}{Race/ethnicity, as a character label or numeric code.
Accepted labels (case-insensitive): \code{"White"}, \code{"Black"}, \code{"Hispanic"},
\code{"Asian"}, \code{"American Indian or Alaska Native"} (plus common aliases such
as \code{"Caucasian"}, \code{"African American"}, \code{"Latino"}, \code{"Unknown"}).
Numeric codes: \code{1} (or \code{0}) White, \code{2} Black, \code{3} Hispanic, \code{4} Asian,
\code{5} American Indian or Alaska Native.}

\item{age_menarche}{Age at menarche in years, or \code{"unknown"}.}

\item{age_first_birth}{Age at first live birth in years, \code{"nulliparous"}
(no live births), or \code{"unknown"}.}

\item{n_biopsies}{Number of prior breast biopsies (\code{0}, \code{1}, \code{2} or more),
or \code{"unknown"} (treated as \code{0}).}

\item{n_relatives}{Number of first-degree relatives (mother, sisters,
daughters) with breast cancer (\code{0}, \code{1}, \code{2} or more), or \code{"unknown"}
(treated as \code{0}).}

\item{atypical_hyperplasia}{Atypical hyperplasia on a prior biopsy:
\code{"yes"}, \code{"no"}, or \code{"unknown"} (default). Ignored when \code{n_biopsies} is
\code{0}/\code{"unknown"}.}

\item{hispanic_nativity}{Only used when \code{race} is Hispanic: \code{"us_born"}
(default) or \code{"foreign_born"}.}

\item{asian_subgroup}{Only used when \code{race} is Asian: \code{"chinese"},
\code{"japanese"}, \code{"filipino"}, \code{"hawaiian"}, \code{"other_pacific_islander"}, or
\code{"other_asian"} (default).}
}
\value{
A numeric vector of 5-year invasive breast cancer probabilities in
\verb{[0, 1]}, one element per (recycled) input row.
}
\description{
Computes the 5-year probability of invasive breast cancer using the NCI
Breast Cancer Risk Assessment Tool (BCRAT), commonly known as the "Gail
model" in its Costantino et al. (1999) form, with the race/ethnicity-
specific extensions for Black (Gail et al. 2007), Hispanic (Banegas et al.
2017), and Asian/Pacific Islander (Matsuno et al. 2011) women. All
arguments are vectorised and recycled to a common length.
}
\details{
The model combines a race-specific relative-risk sub-model with race-
specific baseline hazards in a single-year discrete competing-risk life
table (Gail et al. 1989, equation 1). For each of the five single years of
age from \code{age} to \code{age + 4}:

\deqn{h = F(t) \times RR \times \lambda_1, \quad
      \tau = h + \lambda_2, \quad
      \pi = \frac{h}{\tau}\,e^{-\Sigma\tau}\left(1 - e^{-\tau}\right)}

where \eqn{\lambda_1} and \eqn{\lambda_2} are the race-specific baseline
invasive-breast-cancer incidence and competing (non-breast-cancer)
mortality rates for the five-year age band containing that year, \eqn{RR}
is \code{RR1} (relative risk for ages <50) or \code{RR2} (ages >=50) as appropriate,
\eqn{F(t)} is one minus the attributable-risk fraction, and
\eqn{\Sigma\tau} is the running total hazard from all earlier years. The
5-year risk is the sum of \eqn{\pi} over the five years.

The relative risk is \code{RR1 = exp(LP1)} (ages <50) and
\verb{RR2 = exp(LP1 + biopsies x age50_x_biopsies_beta)} (ages >=50), where

\deqn{LP1 = \beta_{biop}\,NB + \beta_{men}\,AM + \beta_{birth}\,AF +
            \beta_{rel}\,NR + \beta_{birth \times rel}\,AF{\cdot}NR +
            \ln(R_{hyp})}

\code{NB}, \code{AM}, \code{AF}, \code{NR} are the categorised biopsy, menarche, first-birth,
and relative-count covariates (see the parameter descriptions below for
exact category boundaries and race-specific pooling), and \code{R_hyp} is a
fixed multiplier for atypical hyperplasia among biopsied women (\code{0.93} no,
\code{1.82} yes, \code{1.00} unknown or not applicable).

\strong{American Indian/Alaska Native} (and "unknown" race) has no
independently validated relative-risk or baseline-rate model; NCI's
deployed tool substitutes the White model in full, and this package does
the same for exact parity.

\strong{Hispanic} requires a nativity (\code{hispanic_nativity}): US-born and
foreign-born Hispanic women have two entirely distinct fitted models (not
one model with a nativity adjustment), with materially different
coefficients and baseline rates. When not supplied, \code{"us_born"} is used.

\strong{Asian/Pacific Islander} requires a subgroup (\code{asian_subgroup}) for its
baseline rates (the six subgroups share one relative-risk model but have
distinct SEER incidence/mortality tables, and NCI's tool has no pooled
"combined" option). When not supplied, \code{"other_asian"} — the SEER category
for Asian ethnicities not individually tabulated — is used as the closest
available default; specify a subgroup for a more precise estimate.
}
\examples{
# A 50-year-old White woman, average risk factors
bcrat(age = 50, race = "White", age_menarche = 13, age_first_birth = 24,
      n_biopsies = 0, n_relatives = 0)

# A 45-year-old foreign-born Hispanic woman
bcrat(age = 45, race = "Hispanic", hispanic_nativity = "foreign_born",
      age_menarche = 12, age_first_birth = 26, n_biopsies = 0,
      n_relatives = 1)
}
\references{
Gail MH, Brinton LA, Byar DP, et al. Projecting individualized
probabilities of developing breast cancer for white females who are being
examined annually. \emph{J Natl Cancer Inst.} 1989;81(24):1879-1886.
\doi{10.1093/jnci/81.24.1879}

Costantino JP, Gail MH, Pee D, et al. Validation studies for models
projecting the risk of invasive and total breast cancer incidence.
\emph{J Natl Cancer Inst.} 1999;91(18):1541-1548. \doi{10.1093/jnci/91.18.1541}

Gail MH, Costantino JP, Pee D, et al. Projecting individualized absolute
invasive breast cancer risk in African American women.
\emph{J Natl Cancer Inst.} 2007;99(23):1782-1792. \doi{10.1093/jnci/djm223}

Matsuno RK, Costantino JP, Ziegler RG, et al. Projecting individualized
absolute invasive breast cancer risk in Asian and Pacific Islander American
women. \emph{J Natl Cancer Inst.} 2011;103(12):951-961. \doi{10.1093/jnci/djr154}

Banegas MP, John EM, Slattery ML, et al. Projecting individualized absolute
invasive breast cancer risk in US Hispanic women. \emph{J Natl Cancer Inst.}
2017;109(2):djw215. \doi{10.1093/jnci/djw215}
}
